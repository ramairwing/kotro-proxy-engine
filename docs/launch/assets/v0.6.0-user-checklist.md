# Kotro v0.6.0 — user-identical checklist (clean temp HOME)

**Release:** https://github.com/kotro-labs/kotro-proxy-engine/releases/tag/v0.6.0  
**Environment:** `HOME=/tmp/kotro-user-home-clean`, PATH = `~/.local/bin` + system bins only

## Results

| Check | Result |
|-------|--------|
| `releases/latest` → v0.6.0 | PASS |
| `curl \| install.sh` → `~/.local/bin/kotro-proxy` | PASS |
| `--version` prints `kotro-proxy 0.6.0` | PASS |
| SHA-256 of darwin-arm64 `.tar.gz` matches `checksums.txt` | PASS |
| Extension-path sim (latest + checksums + extract) | PASS |
| `/healthz` after ready-wait | PASS |
| `/dashboard` HTTP 200 | PASS |
| `/api/dashboard` HTTP 200 | PASS |
| npm `@kotro-labs/proxy-engine` = 0.6.0 | PASS |
| Release includes `checksums.txt` + VSIX | PASS (curl verified) |
| Homebrew tap = 0.6.0 | PASS after tap bump |
| Marketplace extension 0.6.1 live | NOT VERIFIED (needs publisher) |
| Cursor/Claude Base URL routing | NOT AUTOMATED (manual) |

## Real user gap found

**First boot can take several seconds** while MiniLM loads (`sentence-transformers/all-MiniLM-L6-v2`). A fixed 1–2s sleep after start makes `/healthz` look “down.” Users may think install failed. Mitigation: poll `/healthz` / show “loading model…” in docs/extension.

## Still manual for true end-to-end

1. Publish Marketplace VSIX with first-run / port-conflict UX (after merging guide + extension toast)
2. Follow [docs/guides/CURSOR-FIRST-RUN.md](../../guides/CURSOR-FIRST-RUN.md) — Override Base URL ON, named model, Verify Cache
3. If `AddrInUse`: free `:8080` or change listen addr, then Reload Window
